{"id": "881", "status": "pending", "target": "vm-claude-hub", "command": "cd /home/jose/moreno-ai && python3 -c \"\nimport sys\nfrom pathlib import Path\nsys.path.insert(0, 'runtime')\n\nresults = []\ndef T(l, c, n=''):\n    results.append(('PASS' if c else 'FAIL') + ' ' + l + (': ' + str(n) if n else ''))\n\nfrom agent_identity_loader import load_all_agents\nagents = load_all_agents(repo_root=Path('.'))\ncore = ['sakura','remy','megabyte','larry','sentinel']\nT('M3-count', len([a for a in core if a in agents]) == 5)\nfor name in core:\n    identity = agents[name]\n    has_env = 'environment_awareness' in identity and not identity['environment_awareness'].get('_default')\n    if name == 'sentinel':\n        T('M3-sentinel-env', has_env)\n    else:\n        T('M3-' + name, identity['validation']['passed'] and has_env)\n\nfrom memory_runtime import write_memory, read_memory, write_memory_from_event\nsakura = agents['sakura']\nrec = write_memory(sakura, 'personal', 'Test M4 regression', 'Memory write still works.')\nmid = rec.get('memory_id','?') if rec else 'None'\nT('M4-write', rec is not None and 'memory_id' in rec, mid)\nT('M4-wrapper-exists', callable(write_memory_from_event))\n\ntest_ev = {\n    'event_id':'se-test-m4','event_type':'tunnel_down','source_type':'environment',\n    'severity':'high','ecosystem_time':'2026-06-17T03:00:00+00:00',\n    'subject':{'type':'tunnel','name':'remy.cutegrafiks.com'},'detail':{},'broadcast':False,\n}\nwrapper_rec = write_memory_from_event(sakura, test_ev, 'Tunnel on remy route had a brief outage.', Path('.'))\nT('M4-wrapper-write', wrapper_rec is not None, type(wrapper_rec).__name__)\n\nfrom journal_runtime import write_journal, read_journal, write_journal_from_event\nT('M5-wrapper-exists', callable(write_journal_from_event))\nremy = agents['remy']\njw = write_journal_from_event(remy, test_ev, 'I notice the tunnel had an outage while I was away.', Path('.'))\nT('M5-wrapper-write', jw is not None, type(jw).__name__)\njrec = write_journal(sakura, 'reflection', 'Test M5 regression', 'Journal write still works.')\nT('M5-write', jrec is not None, type(jrec).__name__)\n\nfrom relationship_runtime import read_all_relationships\nsakura_rels = read_all_relationships(sakura)\nT('M6', len(sakura_rels) >= 4, len(sakura_rels))\n\nfrom environment_runtime import get_ecosystem_time\neco_time, source, confidence = get_ecosystem_time(Path('.'))\nT('M7', source == 'audit_trail' and eco_time is not None, source)\n\nprint()\nfor r in results: print(r)\npassed = sum(1 for r in results if r.startswith('PASS'))\nfailed = sum(1 for r in results if r.startswith('FAIL'))\nprint()\nprint('REGRESSION: ' + str(passed) + '/' + str(len(results)) + ' passed, ' + str(failed) + ' failed')\nif failed == 0: print('ALL REGRESSION TESTS PASSED')\n\""}