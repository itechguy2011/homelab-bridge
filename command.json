{"id": "1122", "status": "pending", "target": "vm-memory", "command": "curl -sL https://github.com/nats-io/natscli/releases/download/v0.1.6/nats-0.1.6-linux-amd64.zip -o /tmp/nats.zip && sudo apt-get install -y unzip -qq 2>/dev/null && unzip -o /tmp/nats.zip -d /tmp/nats_extract/ && sudo cp /tmp/nats_extract/nats-0.1.6-linux-amd64/nats /usr/local/bin/nats && sudo chmod +x /usr/local/bin/nats && nats --version && nats -s nats://192.168.0.203:4222 pub memory.created test-from-vm-memory 2>&1 && echo VM_MEMORY_NATS_REACH_OK"}