{"id": 1595, "status": "done", "execution_status": "success", "output": "            result = {\n                \"ok\": True,\n                \"summary\": \"Candidate is validated and awaiting Jose approval\",\n                \"job_id\": job_id,\n                \"run_id\": workspace.name,\n                \"approval_id\": approval_id,\n                \"candidate_package\": str((approval_dir / \"candidate-package.tar.gz\").relative_to(self.repo)),\n                \"candidate_package_sha256\": package_hash,\n                \"rollback_package\": str((approval_dir / \"rollback-baseline.tar.gz\").relative_to(self.repo)),\n                \"approval_statement\": f\"JOSE_APPROVED_LIVE_INSTALL:{job_id}\",\n                \"live_files_modified\": False,\n            }\n            self._audit(\"prepare_candidate_approval\", req, result)\n            return result\n\n\n    def _validate_background_phase_request(self, req: dict[str, Any]) -> str:\n        job_id = self._safe_job_id(req.get(\"job_id\"))\n        job = self._load_job(job_id)\n        if job.get(\"status\") not in {\"blocked\", \"awaiting_approval\"}:\n            raise GovernedError(\n                f\"Job status is not eligible for governed authoring: {job.get('status')}\"\n            )\n        state = self._load_controller_state()\n        if state.get(\"current_job_id\") != job_id:\n            raise GovernedError(\"Controller does not reference the requested job\")\n        objective = req.get(\"objective\")\n        context_paths = req.get(\"context_paths\")\n        target_paths = req.get(\"target_paths\")\n        acceptance = req.get(\"acceptance_checks\", [])\n        if not isinstance(objective, str) or not objective.strip() or len(objective) > 12000:\n            raise GovernedError(\"objective is required and must be <= 12000 characters\")\n        if not isinstance(context_paths, list) or not context_paths:\n            raise GovernedError(\"context_paths is required\")\n        if not isinstance(target_paths, list) or not target_paths:\n            raise GovernedError(\"target_paths is required\")\n        if not isinstance(acceptance, list) or not all(isinstance(x, str) for x in acceptance):\n            raise GovernedError(\"acceptance_checks must be an array of strings\")\n        for raw in context_paths:\n            _, live = self._safe_rel(str(raw), purpose=\"context\", must_exist=True)\n            if not live.is_file():\n                raise GovernedError(f\"Context path is not a file: {raw}\")\n        normalized_targets = [\n            str(self._safe_rel(str(raw), purpose=\"target\")[0])\n            for raw in target_paths\n        ]\n        if len(normalized_targets) != len(set(normalized_targets)):\n            raise GovernedError(\"Duplicate target paths\")\n        if len(normalized_targets) > int(self.limits[\"max_target_files\"]):\n            raise GovernedError(\"Too many target files\")\n        for field in (\"max_attempts\", \"max_validation_cycles\", \"timeout_seconds\"):", "exit_code": 0}