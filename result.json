{"id": "1295", "output": "=== FULL CODE NODE ===\nconst item = $input.first().json;\n\nconst headers = item.headers || {};\nconst body = item.body || item;\n\n// ============================================================\n// ChatGPT Read Write Bridge v1\n// Moreno AI Ecosystem\n// READ + WRITE + DIFF + COMMIT\n// ============================================================\n//\n// SETUP REQUIRED:\n// 1. Replace CHANGE_ME with a NEW write bridge secret.\n// 2. Select vm-memory SSH credentials on the SSH node.\n// 3. Save and activate/publish workflow.\n//\n// IMPORTANT:\n// - This is NOT arbitrary shell access.\n// - All actions are allowlisted.\n// - All writes are restricted to approved repo paths.\n// - Dangerous paths and secret-like files are blocked.\n// - Commit does not push.\n// - Push is intentionally not included.\n//\n// Repo target:\n// /home/jose/moreno-ai\n//\n// ============================================================\n\nconst EXPECTED_SECRET = 'N6UtHFxTrWYjAwdPSQM943uBiJVGsEqXOIakho0z';\n\nconst providedSecret =\n  headers['x-bridge-secret'] ||\n  headers['X-Bridge-Secret'] ||\n  headers['x_bridge_secret'] ||\n  '';\n\nconst action = body.action || 'read_status';\nconst targetPath = body.target_path || body.path || '';\nconst content = body.content;\nconst contentB64 = body.content_b64;\nconst commitMessage = body.message || 'ChatGPT ecosystem update';\nconst tagName = body.tag || '';\nconst maxReadLines = Number(body.max_lines || 400);\n\n// ---------- Helpers ----------\n\nfunction shQuote(value) {\n  return \"'\" + String(value).replace(/'/g, \"'\\\\''\") + \"'\";\n}\n\nfunction pyString(value) {\n  return JSON.stringify(String(value));\n}\n\nfunction safeEcho(text) {\n  return `printf '%s\\n' ${shQuote(text)}`;\n}\n\nfunction isPlainObject(value) {\n  return value !== null && typeof value === 'object' && !Array.isArray(value);\n}\n\nfunction normalizePath(p) {\n  return String(p || '').replace(/^\\/+/, '').replace(/\\\\/g, '/');\n}\n\nconst allowedPrefixes = [\n  'docs/',\n  'runtime/',\n  'tests/',\n  'memory/council/',\n  'scripts/',\n  'tools/'\n];\n\nconst blockedExact = [\n  '.env',\n  '.git',\n  '.gitignore.lock',\n  'secrets',\n  'credentials',\n  'id_rsa',\n  'id_ed25519'\n];\n\nconst blockedContains = [\n  '/.git/',\n  '/node_modules/',\n  '/__pycache__/',\n  '/.venv/',\n  '/venv/',\n  '/env/',\n  '/secrets/',\n  '/credentials/'\n];\n\nconst blockedExtensions = [\n  '.pem',\n  '.key',\n  '.token',\n  '.p12',\n  '.pfx',\n  '.crt',\n  '.csr',\n  '.der',\n  '.sqlite',\n  '.db'\n];\n\nfunction validateTargetPath(p, allowDirectory = false) {\n  const rel = normalizePath(p);\n\n  if (!rel) {\n    return { ok: false, path: rel, error: 'Missing target_path' };\n  }\n\n  if (rel.includes('..')) {\n    return { ok: false, path: rel, error: 'Blocked path traversal: .. is not allowed' };\n  }\n\n  if (rel.startsWith('/')) {\n    return { ok: false, path: rel, error: 'Absolute paths are not allowed' };\n  }\n\n  if (blockedExact.includes(rel) || blockedExact.includes(rel.replace(/\\/$/, ''))) {\n    return { ok: false, path: rel, error: 'Blocked exact path' };\n  }\n\n  for (const part of blockedContains) {\n    if (('/' + rel + '/').includes(part)) {\n      return { ok: false, path: rel, error: `Blocked path segment: ${part}` };\n    }\n  }\n\n  for (const ext of blockedExtensions) {\n    if (rel.toLowerCase().endsWith(ext)) {\n      return { ok: false, path: rel, error: `Blocked file extension: ${ext}` };\n    }\n  }\n\n  const allowed = allowedPrefixes.some(prefix => rel === prefix.replace(/\\/$/, '') || rel.startsWith(prefix));\n  if (!allowed) {\n    return {\n      ok: false,\n      path: rel,\n      error: `Path not in allowed prefixes: ${allowedPrefixes.join(', ')}`\n    };\n  }\n\n  if (!allowDirectory && rel.endsWith('/')) {\n    return { ok: false, path: rel, error: 'target_path must be a file path, not a directory path' };\n  }\n\n  return { ok: true, path: rel, error: null };\n}\n\nfunction encodeContentToB64() {\n  if (typeof contentB64 === 'string' && contentB64.length > 0) {\n    const trimmed = contentB64.trim();\n    if (!/^[A-Za-z0-9+/=\\r\\n]+$/.test(trimmed)) {\n      return { ok: false, b64: '', error: 'content_b64 contains invalid characters' };\n    }\n    return { ok: true, b64: trimmed.replace(/\\s+/g, ''), error: null };\n  }\n\n  if (typeof content === 'string') {\n    return {\n      ok: true,\n      b64: Buffer.from(content, 'utf8').toString('base64'),\n      error: null\n    };\n  }\n\n  return { ok: false, b64: '', error: 'Missing content or content_b64' };\n}\n\nfunction writeFileCommand(rel, b64, mode) {\n  const append = mode === 'append';\n  return `cd /home/jose/moreno-ai && python3 - <<'PY'\nimport base64\nimport pathlib\nimport shutil\nimport time\n\nrepo = pathlib.Path('/home/jose/moreno-ai').resolve()\nrel = ${pyString(rel)}\ntarget = (repo / rel).resolve()\n\nif repo != target and repo not in target.parents:\n    raise SystemExit('BLOCKED: target escapes repo')\n\ntarget.parent.mkdir(parents=True, exist_ok=True)\n\nbackup = None\nif target.exists() and target.is_file():\n    ts = time.strftime('%Y%m%d-%H%M%S')\n    backup = repo / '.chatgpt_backups' / ts / rel\n    backup.parent.mkdir(parents=True, exist_ok=True)\n    shutil.copy2(target, backup)\n\ndata = base64.b64decode(${pyString(b64)}).decode('utf-8')\n\nif ${append ? 'True' : 'False'}:\n    with target.open('a', encoding='utf-8', newline='') as f:\n        f.write(data)\nelse:\n    target.write_text(data, encoding='utf-8', newline='')\n\nprint('OK')\nprint('operation=${append ? 'append_file' : 'write_file'}')\nprint('target=' + rel)\nprint('backup=' + (str(backup.relative_to(repo)) if backup else 'none'))\nprint('bytes=' + str(len(data.encode('utf-8'))))\nPY`;\n}\n\nfunction readFileCommand(rel, lines) {\n  const safeLines = Math.min(Math.max(Number(lines || 400), 1), 2000);\n  return `cd /home/jose/moreno-ai && python3 - <<'PY'\nimport pathlib\n\nrepo = pathlib.Path('/home/jose/moreno-ai').resolve()\nrel = ${pyString(rel)}\ntarget = (repo / rel).resolve()\n\nif repo != target and repo not in target.parents:\n    raise SystemExit('BLOCKED: target escapes repo')\n\nif not target.exists():\n    raise SystemExit('NOT_FOUND: ' + rel)\n\nif target.is_dir():\n    raise SystemExit('IS_DIRECTORY: ' + rel)\n\nmax_lines = ${safeLines}\nwith target.open('r', encoding='utf-8', errors='replace') as f:\n    for i, line in enumerate(f, 1):\n        if i > max_lines:\n            print('--- TRUNCATED AT LINE ' + str(max_lines) + ' ---')\n            break\n        print(line, end='')\nPY`;\n}\n\nfunction mkdirCommand(rel) {\n  return `cd /home/jose/moreno-ai && python3 - <<'PY'\nimport pathlib\n\nrepo = pathlib.Path('/home/jose/moreno-ai').resolve()\nrel = ${pyString(rel)}\ntarget = (repo / rel).resolve()\n\nif repo != target and repo not in target.parents:\n    raise SystemExit('BLOCKED: target escapes repo')\n\ntarget.mkdir(parents=True, exist_ok=True)\nprint('OK')\nprint('operation=mkdir')\nprint('target=' + rel)\nPY`;\n}\n\nfunction pyCompileCommand(rel) {\n  return `cd /home/jose/moreno-ai && python3 -m py_compile ${shQuote(rel)}`;\n}\n\nfunction gitDiffCommand(rel) {\n  if (rel) {\n    return `cd /home/jose/moreno-ai && git diff -- ${shQuote(rel)} && printf '\\\\n=== STAT ===\\\\n' && git diff --stat -- ${shQuote(rel)}`;\n  }\n  return `cd /home/jose/moreno-ai && git diff && printf '\\\\n=== STAT ===\\\\n' && git diff --stat`;\n}\n\nfunction gitCommitAllowedCommand(message) {\n  const msg = String(message || 'ChatGPT ecosystem update').slice(0, 160);\n  return `cd /home/jose/moreno-ai && printf '=== BEFORE STATUS ===\\\\n' && git status --short && git add docs runtime tests memory/council scripts tools && printf '\\\\n=== CACHED DIFF STAT ===\\\\n' && git diff --cached --stat && printf '\\\\n=== COMMIT ===\\\\n' && git commit -m ${shQuote(msg)} && printf '\\\\n=== AFTER STATUS ===\\\\n' && git status --short --branch`;\n}\n\nfunction createTagCommand(tag) {\n  const t = String(tag || '').trim();\n  if (!/^[A-Za-z0-9._-]{1,80}$/.test(t)) {\n    return { ok: false, command: safeEcho('Invalid tag. Use only letters, numbers, dots, underscores, and hyphens. Max 80 chars.') };\n  }\n  return {\n    ok: true,\n    command: `cd /home/jose/moreno-ai && git tag ${shQuote(t)} && git tag --list $\n", "status": "done"}